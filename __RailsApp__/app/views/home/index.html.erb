<div class="welcome-container">
  <div class="animated-background">
    <div class="blob blob-1"></div>
    <div class="blob blob-2"></div>
    <div class="blob blob-3"></div>
    <div class="blob blob-4"></div>
    <div class="blob blob-5"></div>
  </div>

  <div class="content">
    <h1 class="title">Welcome to "Rails Start!"</h1>
    
    <div class="versions">
      <p><strong>Rails version:</strong> <%= @rails_version %></p>
      <p><strong>Rack version:</strong> <%= @rack_version %></p>
      <p><strong>Ruby version:</strong> <%= @ruby_description %></p>
      <p><strong>YJIT:</strong> <%= @yjit_enabled ? 'enabled' : 'disabled' %></p>
      <p><strong>ZJIT:</strong> <%= @zjit_enabled ? 'enabled' : 'disabled' %></p>
      <p><strong>Cache Enabled:</strong> <%= @cache_enabled ? 'Yes' : 'No' %></p>
      <p><strong>Environment:</strong> <%= Rails.env %></p>
    </div>
  </div>
</div>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body, html {
    height: 100%;
    overflow: hidden;
  }

  .welcome-container {
    position: relative;
    width: 100vw;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    overflow: hidden;
  }

  .animated-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    overflow: hidden;
    z-index: 1;
  }

  .blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.7;
    animation-timing-function: ease-in-out;
    animation-iteration-count: infinite;
    animation-direction: alternate;
    will-change: transform;
  }

  .blob-1 {
    width: 500px;
    height: 500px;
    background: linear-gradient(135deg, #FFB6C1, #FFC0CB);
    top: -15%;
    left: -15%;
    animation: float-1 8s infinite;
  }

  .blob-2 {
    width: 600px;
    height: 600px;
    background: linear-gradient(135deg, #B0E0E6, #87CEEB);
    top: 40%;
    right: -20%;
    animation: float-2 8s infinite;
  }

  .blob-3 {
    width: 450px;
    height: 450px;
    background: linear-gradient(135deg, #DDA0DD, #E6E6FA);
    bottom: -15%;
    left: 15%;
    animation: float-3 8s infinite;
  }

  .blob-4 {
    width: 550px;
    height: 550px;
    background: linear-gradient(135deg, #FFE4B5, #FFDAB9);
    top: 10%;
    right: 20%;
    animation: float-4 8s infinite;
  }

  .blob-5 {
    width: 480px;
    height: 480px;
    background: linear-gradient(135deg, #B0E57C, #C1FFC1);
    bottom: 10%;
    right: 5%;
    animation: float-5 8s infinite;
  }

  @keyframes float-1 {
    0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); }
    50% { transform: translate(1000px, 1250px) scale(1.3) rotate(180deg); }
  }

  @keyframes float-2 {
    0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); }
    50% { transform: translate(-1250px, -900px) scale(1.2) rotate(-180deg); }
  }

  @keyframes float-3 {
    0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); }
    50% { transform: translate(1100px, -1000px) scale(1.25) rotate(180deg); }
  }

  @keyframes float-4 {
    0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); }
    50% { transform: translate(-900px, 1100px) scale(1.35) rotate(-180deg); }
  }

  @keyframes float-5 {
    0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); }
    50% { transform: translate(-1200px, 750px) scale(1.15) rotate(180deg); }
  }

  .content {
    position: relative;
    z-index: 10;
    text-align: center;
    color: #2c3e50;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
  }

  .title {
    font-size: 4rem;
    font-weight: 700;
    margin-bottom: auto;
    margin-top: auto;
    text-shadow: 2px 2px 4px rgba(255, 255, 255, 0.8);
    letter-spacing: 2px;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
  }

  .versions {
    margin-bottom: 3rem;
    font-size: 1.2rem;
    line-height: 1.8;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
  }

  .versions p {
    margin: 0.1rem 0;
    font-weight: 400;
  }

  .versions strong {
    font-weight: 700;
  }

  @media (max-width: 768px) {
    .title {
      font-size: 2.5rem;
    }

    .versions {
      font-size: 1rem;
      padding: 1.5rem 2rem;
    }

    .blob {
      filter: blur(60px);
    }
  }
</style>
