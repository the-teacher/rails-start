<div class="ah-container">
  <aside class="ah-sidebar">
    <p class="ah-sidebar-title">Agent Events</p>
    <div id="ah-events" class="ah-events"></div>
  </aside>

  <div class="ah-main">
    <div class="ah-card">
      <div class="ah-logo">&#x26A1;</div>
      <h1 class="ah-title">Case 04 &mdash; RubyLLM Backend</h1>
      <p class="ah-subtitle">Streaming + lifecycle events via the <code>ruby_llm</code> gem backend</p>

      <%= render "form" %>
      <%= render "result" %>
      <%= render "nav",
           back_label: "&larr; Case 03",
           back_path:  ai_agents_lifecycle_path,
           next_label: "Case 05: Fallback &rarr;",
           next_path:  ai_agents_fallback_path %>
    </div>
  </div>
</div>

<% content_for :scripts do %>
  <%= javascript_include_tag "ai_agent_ruby_llm" %>
<% end %>
