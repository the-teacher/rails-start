<div class="ah-case-card ah-case-card--featured">
  <div class="ah-case-number">05</div>
  <h2 class="ah-case-name">Fallback Chain</h2>
  <p class="ah-case-desc">
    Two broken models are prepended via <code>models.prepend</code> at runtime.
    Watch the sidebar as each one fails and the chain falls through to the first working model.
  </p>
  <ul class="ah-case-tags">
    <li>models.prepend</li>
    <li>retry events</li>
    <li>fallback chain</li>
  </ul>
  <%= link_to "Try it →", ai_agents_fallback_path, class: "ah-case-btn ah-case-btn--featured" %>
</div>
